/** Minimal vscode stub for unit tests that import services using the API. */
export const window = {
  activeTextEditor: undefined,
  showErrorMessage: async () => undefined,
  showInformationMessage: async () => undefined,
};

export default { window };
